<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE xml>
<configuration debug="true">
	<include
		resource="org/springframework/boot/logging/logback/defaults.xml" />
	<springProperty scope="context" name="applicationName"
		source="spring.application.name" />
	<springProperty scope="context" name="logstash-server"
		source="spring.logstash.server" />
	<!-- Example for logging into the build folder of your project -->
	<property name="FILE_LOG_PATTERN"
		value="%d{yyyy-MM-dd HH:mm:ss.SSS} -- [${applicationName}] -- %5p ${PID:- } --- [%t] [%X{sessionId}] %-40.40logger{39} : %m%n%wex" />


	<property name="CONSOLE_LOG_PATTERN"
		value="%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) [${applicationName},%X{X-B3-TraceId:-},%X{X-B3-SpanId:-}] %highlight(%-5level) %logger{36}.%M -%X{request-id}- %msg%n" />

	<logger
		name="org.springframework.web.filter.CommonsRequestLoggingFilter">
		<level value="DEBUG" />
	</logger>

	<appender name="console"
		class="ch.qos.logback.core.ConsoleAppender">
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>DEBUG</level>
		</filter>
		<encoder>
			<pattern>${CONSOLE_LOG_PATTERN}</pattern>
			<charset>utf8</charset>
		</encoder>
	</appender>

	<root level="INFO">
		<appender-ref ref="console" />
		<!--<appender-ref ref="logstash"/> -->
	</root>

</configuration>